# EssentialBreathingWeb

A lightweight web version of EssentialBreathing.

This version is built with plain HTML, CSS and JavaScript, using SVG for the breathing animation.  
It is intended to be simple, fast, mobile-friendly and independent from the Godot version.